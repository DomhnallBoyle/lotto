Generating club lotto numbers

```
pip install -r requirements.txt

# to find club jackpots
python find_jackpots.py

# to get previously drawed numbers
python find_numbers.py <club_url>

# to generate automatically
python generate_numbers.py <club_url> <num_lines> <num_numbers> <top_n> 
```
